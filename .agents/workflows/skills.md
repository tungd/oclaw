---
description: How to add skill support
---

Here is a general outline of how to add skill functionality:
1. Examine existing system (which seems to use `tools.ml`, string commands running via Unix CLI etc.).
2. Define a "skill" module: potentially a way to group a set of related tools. 
